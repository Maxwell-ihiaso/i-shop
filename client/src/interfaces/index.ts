export interface ISliderItem {
  id: number;
  img: string;
  title: string;
  desc: string;
  bg: string;
}

export interface ICategory {
  id: number;
  img: string;
  title: string;
  cat: string;
}

export interface IPopularProduct {
  _id: string;
  main_image: string;
}
